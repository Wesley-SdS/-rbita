node_modules
.pnpm-store
.turbo
dist
.next
out
build
coverage

# env
.env
.env.local
.env.*.local

# MCP local — contém chaves de API (Render etc). NUNCA commitar: o repo é público.
.mcp.json

# db
pgdata

# os / editor
.DS_Store
Thumbs.db
*.log

# python
.venv
__pycache__
*.pyc

# voz — modelos baixados sob demanda + amostras de áudio
apps/voice/models_test
apps/voice/models
*.wav

# harness/editor local (Claude Code). As SKILLS são versionadas de propósito:
# são convenção do projeto, valem para qualquer máquina e para qualquer sessão.
# O resto (launch.json, settings.local.json) é pessoal e fica de fora.
.claude/*
!.claude/skills/
